app.controller("AdminCtrl", function($scope, $route, $location, $modal, $rootScope, SiteService, UserService, Logger, Stats) {
   // var stats = Stats.getInstance("admin");
    //var logger = Logger.getInstance("admin");
    
   
});